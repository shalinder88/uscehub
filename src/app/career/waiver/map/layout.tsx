import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "J-1 Waiver Interactive Map — Conrad 30 by State — USCEHub",
  description: "Interactive map showing Conrad 30 waiver slot availability, fill patterns, and state-by-state intelligence for J-1 visa physicians.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
