import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "J-1 Waiver Process Step-by-Step — All 6 Pathways — USCEHub",
  description: "Visual step-by-step process for all J-1 waiver pathways: Conrad 30, HHS, ARC, DRA, SCRC, and VA. Forms, agencies, and timelines at each step.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
