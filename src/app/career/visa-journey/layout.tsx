import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Physician Visa Journey Timeline — J-1 to Green Card — USCEHub",
  description: "Complete immigration timeline from J-1 training through waiver, H-1B, and green card for international medical graduate physicians.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
